name: Scrape and Upload

on:
  workflow_dispatch:

jobs:
  carrefour-bot:
    name: Test puppeteer
    runs-on: ubuntu-latest
    env:
      API_ENDPOINT: "${{ secrets.API_ENDPOINT }}" # Set the environment variable for the job

    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "latest"

      - name: Install packages
        working-directory: scripts/carrefour
        run: npm ci

      - name: Install packages
        working-directory: scripts/carrefour
        run: npm install

      - name: Run Puppeteer Script
        uses: tj-actions/puppeteer@v5
        id: puppeteer-test
        with:
          files: scripts/carrefour/**/*
        env:
          API_ENDPOINT: ${{ secrets.API_ENDPOINT }} # Ensure the script has access to the environment variable
